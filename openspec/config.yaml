# SDD Configuration — audio-edit
# Generated by sdd-init skill

project:
  name: audio-edit
  root: C:\My_Projects\Tests\audio-edit
  mode: hybrid  # engram + openspec

detected_stack:
  package_manager: npm
  language: JavaScript (ES modules)
  runtime: browser (vanilla JS, no framework)
  build: none (static HTML/JS served via python http.server)
  key_dependencies: []
  project_type: audio editing tool (WAV, browser-based, local)

architecture:
  src/
    app/          — Application entry point and shell
    audio/        — Audio processing (decode, encode, trim, concatenate, waveform)
    components/   — UI components (reactive patterns via vanilla JS)
    domain/       — Domain models (AudioSegment, AudioTrack)
    utils/        — Utility functions (file validation)
    main.js       — Bootstrap
    styles.css    — Global styles

testing:
  test_runner: null
  test_framework: null
  coverage_tool: null
  linter: null
  formatter: null
  e2e_tests: null
  status: NOT DETECTED (no test infrastructure found)
  note: >
    This project has no test scripts, jest.config, vitest.config,
    tsconfig.json, .eslintrc, or prettier config. Testing is not set up.

strict_tdd:
  enabled: false
  reason: >
    No test framework detected. Strict TDD mode requires a working test runner
    (jest, vitest, or equivalent) to enforce red-green-refactor cycles.

sd_lifecycle:
  phases:
    - propose
    - spec
    - design
    - tasks
    - apply
    - verify
    - archive

persistence:
  engram:
    memory_project: C:\My_Projects\Tests\audio-edit
    topic_keys:
      - sdd-init/audio-edit
      - sdd/audio-edit/testing-capabilities
      - skill-registry
  openspec:
    root: C:\My_Projects\Tests\audio-edit\openspec
    config_file: C:\My_Projects\Tests\audio-edit\openspec\config.yaml
    specs_dir: C:\My_Projects\Tests\audio-edit\openspec\specs
    changes_dir: C:\My_Projects\Tests\audio-edit\openspec\changes
    archive_dir: C:\My_Projects\Tests\audio-edit\openspec\changes\archive

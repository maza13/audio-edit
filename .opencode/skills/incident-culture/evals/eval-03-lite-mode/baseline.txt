The formatting skill likely lacks null input validation, causing a null reference exception when processing null data. Adding a null check at the entry point would prevent the crash and return a graceful error instead.

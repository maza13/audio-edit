The search likely failed because the pattern was treated too narrowly, so `config*.json` was matched only at the top level or as an exact-looking token instead of a recursive glob. The implementation may not be expanding subdirectories before filtering, which would miss valid files deeper in the tree. Another possibility is that the search pipeline normalizes the query in a way that strips wildcard semantics. The fix is to verify glob handling, ensure recursion is enabled, and add a test with a matching file in a nested directory.

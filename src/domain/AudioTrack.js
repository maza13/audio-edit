/**
 * @typedef {Object} AudioTrack
 * @property {string} id
 * @property {File} file
 * @property {string} name
 * @property {number} size
 * @property {string} type
 * @property {AudioBuffer | null} buffer
 * @property {number | null} duration
 * @property {number | null} numberOfChannels
 * @property {number} trimStart
 * @property {number | null} trimEnd
 * @property {"pending" | "decoding" | "ready" | "error"} decodeStatus
 * @property {string | null} decodeError
 * @property {boolean} [isVideo]
 */

export {};
